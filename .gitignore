# macOS metadata
.DS_Store

# ffmpeg 2-pass encoding artifacts (produced during video compression)
ffmpeg2pass-*
*.log
*.mbtree

# Raw video sources — commit ONLY the web-optimized versions.
# The raw uploads (e.g. CD5_SP26_David Gomez_Motion Trailer.mp4)
# are over GitHub's 100 MB push limit; keep them local, commit the
# compressed trailer.mp4 that lives alongside.
CD5_SP26_*
*_Motion Trailer.mp4
*_MotionTrailer.mp4
